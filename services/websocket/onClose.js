const onClose = async (ws, wss, WebSocket, users) => {
  //   ws.on("close", function (data) {
  //     console.log("user is leaving");
  //     users.map((user) => {
  //       deleteUser(user);
  //     });
  //   });
};

module.exports = { onClose };
